{
  "dependencies": {
    "@xata.io/client": "^0.30.1",
    "dotenv": "^16.5.0",
    "express": "^5.1.0"
  },
  "scripts": {
    "build": "echo 'No build step needed for Node.js apps'",
    "start": "node api/query.js"  // Adjust if your entry file is different
  }
}
